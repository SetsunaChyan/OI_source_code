#include <bits/stdc++.h>
using namespace std;

int ans,n,mp[2005][6005],x,y,d;
char s[10];

int main()
{
    scanf("%d",&n);
    while(n--)
    {
        scanf("%s%d%d%d",s,&x,&y,&d);
        x+=1000,y+=y*2+3000;
        if(s[0]=='A')
        {
            for(int i)
        }
        else
        {

        }
    }
    for(int i=0;i<=2000;i++)
        for(int j=0;j<=6002;j++)
    if(ans&1) printf("%d.50",ans/2);
    else printf("%d.00",ans/2);
    return 0;
}
